# Normalize text files to LF in the repo; editors on any OS can handle LF.
* text=auto eol=lf

# If you ever add Windows-specific scripts, keep them CRLF:
*.bat text eol=crlf
*.ps1 text eol=crlf

# Keep images/binaries as-is
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
